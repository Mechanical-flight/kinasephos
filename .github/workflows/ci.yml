name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install --upgrade pip
      - name: Install CPU PyTorch
        run: python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
      - run: python -m pip install -e ".[dev]"
      - run: ruff check .
      - run: ruff format --check .
      - run: pytest -q
      - name: CPU smoke train
        run: |
          python scripts/generate_demo_data.py --output data/processed/demo.csv
          python scripts/train.py --config configs/train_smoke.yaml
      - name: Synthetic evaluation
        run: python scripts/evaluate.py --config configs/evaluate.yaml
      - name: Synthetic inference
        run: python scripts/predict.py --input examples/demo_input.csv --output outputs/demo_predictions.csv
      - name: Student-only export
        run: python scripts/export_student.py --checkpoint outputs/smoke/best.pt --output outputs/export
