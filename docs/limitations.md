# Limitations

See the README and model/data cards. In particular, absent annotation is not an absolute negative;
local sequence omits distant structure and context; broad family labels are incomplete; database and
mapping biases remain; and predictions require experimental validation. RFdiffusion, ProteinMPNN,
and AlphaFold integration are future work only.

