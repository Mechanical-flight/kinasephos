# Reproducibility

Pin the input hashes, source releases, mapping dictionaries, configuration, seed, CD-HIT version and
command, environment, Git commit, teacher model/revision, tokenizer, pooling rule, cache manifest, and
split membership. Re-run `ruff check .`, `ruff format --check .`, and `pytest -q` before release.

