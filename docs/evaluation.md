# Evaluation

Evaluate only a leakage-free held-out split. Report binary and valid-family metrics separately, plus
S/T/Y, family, source, source-count, padding, protein, and cluster strata when real metadata exists.
Save false positives, false negatives, family errors, ambiguous sites, low-confidence records,
unmapped accessions, and coordinate conflicts. Do not promote smoke metrics to a Results claim.

