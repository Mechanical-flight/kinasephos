# Dataset lineage

The supplied files were independently hashed and inspected. The ZIP and its extracted member are one
source asset, not two datasets. The GZIP and decompressed workbook are likewise one source asset. No
evidence establishes that the FASTA, EPSD-schema relation table, and multi-organism workbook are the
same release family, so they are not silently merged. Derived validated windows remain outside Git.
