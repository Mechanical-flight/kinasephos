# Contributing

Open an issue before large changes. Keep raw or restricted data outside Git, add tests, run
`ruff check .`, `ruff format --check .`, and `pytest -q`, and clearly distinguish synthetic smoke
results from independently reproduced metrics.

