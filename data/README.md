# Data directory

Do not commit raw database exports. `raw`, `interim`, and `processed` are ignored except for directory
placeholders. Use synthetic fixtures for tests and obtain real data under provider terms.

